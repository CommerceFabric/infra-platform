apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  namespace: commercefabric-namespace
  labels:
    app: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:8.10.0
          ports:
            - containerPort: 6379
